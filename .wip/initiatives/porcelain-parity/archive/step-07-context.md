# Step 7 — Rolling Context (archived 2026-07-12)

**Coordinator**: porcelain-parity-step-07-coordinator (process 876)
**Researcher**: porcelain-parity-step-07-researcher (process 877)
**Workplan**: .wip/initiatives/porcelain-parity/workplans/step-07-the-parity-drift-guard.md (archived alongside this file as step-07-workplan.md)
**Build started**: 2026-07-12

## Batching plan

| Batch | Chunks | Files | Rationale |
|---|---|---|---|
| Builder 1 | Chunk 1 | test/parity.tsv | Manifest first; everything else reads it |
| Builder 2 | Chunk 2 | test/test-parity.sh (assertions 1-3, bidirectional) | Core harness + skeleton |
| Builder 3 | Chunks 3+4+5 | test/test-parity.sh (assertions 4,5,6) | Independent, mechanically simple; same file, one builder for efficiency, 3 separate commits |
| Builder 4 | Chunk 6 | test/parity-known-red.tsv + test/test-parity.sh (baseline/self-expiry logic) | Most complex; depends on assertions 1,5,6 already existing |
| Builder 5 | Chunk 7 | test/test-clast.sh (suites=() registration) | Final wiring; make test must be fully green after this |
| Coordinator (direct) | Chunk 8 | none shipped (temp edit + revert) | Fail-closed proof, done directly via Bash, not a Builder |

## Decisions made during build

- Orchestrator (807) ruling: baseline+ratchet (test/parity-known-red.tsv, self-expiring), NOT ship-red. make test/lint fully green at every commit, achievable once the baseline chunk lands.
- Assertion 1 is bidirectional (manifest ⊆ --help AND --help ⊆ manifest) for mirrored rows on subcommands with a usage_fn — this is how CLAST_WAKE_AUTODISMISS_NOOP's heredoc gap becomes mechanically checkable without a 7th assertion.
- `make lint` shellchecks every test/*.sh unconditionally (Makefile:6-8) — test/test-parity.sh must pass shellcheck from its FIRST commit, even before suites=() wiring. `make test` only runs suites in the array, so test-parity.sh's own correctness isn't exercised by `make test` until Builder 5's chunk lands — intermediate builders validate via `bash test/test-parity.sh` directly.
- 7 known baseline rows at landing: 2x BDS-100 (AskUserQuestion overflow, skills/wake/SKILL.md), 1x BDS-101 (CLAST_WAKE_AUTODISMISS_NOOP missing from wake --help), 4x BDS-101 (CLAST_AUTHOR/CLAST_MACHINE/CLAST_VERBOSE/CLAST_RETRO_PROGRESS undocumented in docs/reference/config.md).

## Escalations

**[ESCALATION step-07/builder-03]** (2026-07-12, during Task 3/todo 544): Assertion 5's literal Decision-6 scoping surfaces 7 undocumented CLAST_* vars, not the expected 4 — CLAST_LIB, CLAST_JSON, and CLAST_COMMAND_MARKER_RE's read site are genuine matches not covered by the two named exclusions (_SOURCED family, CLAST_NOW_EPOCH), despite the workplan's own narrative calling CLAST_LIB/CLAST_JSON "not meant to be user-set." Coordinator independently verified via grep — Builder-03's finding is accurate. Routed to Orchestrator (807) with recommendation: extend the permanent exclusion list (same treatment as CLAST_NOW_EPOCH) rather than baseline all 7. Build paused on Task 3 pending response; Builder-03 (process 881) left open.

**Resolution (Orchestrator, 807):** Agreed CLAST_LIB/CLAST_JSON/CLAST_COMMAND_MARKER_RE are permanent exemptions (not baseline debt — nothing to fix). Amendment: exemptions become a new `internal` manifest kind in test/parity.tsv (data, not hardcoded bash), including moving CLAST_NOW_EPOCH there too for consistency, each self-expiring if its var stops being read anywhere. `_SOURCED` family stays a code-level pattern (unenumerable). Net: assertion 5 finds exactly the original 4 violations, baselined under BDS-101. Workplan Decision 6 updated to match. Builder-03 explicitly commended for escalating instead of force-fitting. Resumed Builder-03 with amended Commit B scope (also touched test/parity.tsv to add 4 `internal` rows).

## Per-task outcomes

**Task 1 (todo 542, Builder-01/878) — test/parity.tsv manifest: DONE.** Builder created a 24-line manifest (8-line header comment + 16 data rows) covering wake (flag `--auto`; env `CLAST_WAKE_SINCE`/`CLAST_WAKE_AUTO_MIN_CHARS`/`CLAST_WAKE_AUTODISMISS_NOOP`; skill_only `promote-decision-common-issue-workflow` + `model-call-timing`), retro (6 flags), undismiss (cli-only), and a global `*`-subcommand env row for `CLAST_LLM_*`. Coordinator independently Read the full file and confirmed content matches the spec exactly, and confirmed via `git fetch origin && git log --oneline origin/beau/bds-82-porcelain-parity..HEAD` (empty output) that commit `bff33db957dd5ec5650b9a9048f4a025a76478cf` arrived at origin. make test/make lint were green before commit. No escalations.

**Task 2 (todo 543, Builder-02/879) — test/test-parity.sh assertions 1-3: DONE.** Builder created a 215-line test-parity.sh implementing the bidirectional --help<->manifest diff (assertion 1), mirrored-in-SKILL.md check (assertion 2), and cli-only/skill_only reason check (assertion 3), function-level style sourcing the porcelain libs directly. Coordinator independently re-ran `bash test/test-parity.sh` (31 passed, 1 failed — exactly the expected/correct CLAST_WAKE_AUTODISMISS_NOOP direction-B violation, not yet baselined) and `direnv exec . make lint` (exit 0, confirmed properly this time after catching a pipe-exit-code bug in my own first check). Commit e7ba5130296c2053f366dcd0a346432329e1edec confirmed arrived at origin via empty `git log origin/beau/bds-82-porcelain-parity..HEAD`. No escalations.

**Task 3 (todo 544, Builder-03/881) — assertions 4, 5, 6: DONE, with one escalation along the way.** Builder-03 correctly escalated when assertion 5's literal scoping surfaced 7 undocumented vars instead of the expected 4 (CLAST_LIB/CLAST_JSON/CLAST_COMMAND_MARKER_RE weren't covered by the two named exclusions despite the workplan's prose already calling them dispatcher-internal). Orchestrator ruled: permanent exemptions become a new self-expiring `internal` manifest kind in test/parity.tsv (not hardcoded bash), covering CLAST_LIB/CLAST_JSON/CLAST_COMMAND_MARKER_RE/CLAST_NOW_EPOCH. Workplan Decision 6 updated to match. Builder-03 resumed and landed 3 commits: 76b8057 (assertion 4), ffe5955 (assertion 5 + internal exemption rows + self-expiry), e06f06c (assertion 6). Coordinator independently verified: Read both test/parity.tsv (20 data rows, 4 new internal rows each with a real reason) and test/test-parity.sh (359 lines) in full; re-ran `bash test/test-parity.sh` and got the exact expected 54 passed/7 failed (1× assertion-1 CLAST_WAKE_AUTODISMISS_NOOP, 4× assertion-5 undocumented vars, 2× assertion-6 AskUserQuestion overflows — all real, none yet baselined); confirmed `direnv exec . make lint` exit 0 and `make test` prints "all test suites passed" (test-parity.sh still not wired into suites(), as expected — Task 5 does that); confirmed all 3 commits arrived at origin via empty `git log origin/beau/bds-82-porcelain-parity..HEAD`. No further escalations.

**Task 4 (todo 545, Builder-04/882) — self-expiring baseline mechanism: DONE.** Builder created test/parity-known-red.tsv (7 rows: assertion/scope/identifier/ticket, derived directly from what assertions 1/5/6 already compute internally so the two can't drift) and wired `_parity_baseline_check` + `assert_parity_baseline_self_expiry` into test/test-parity.sh. Commit af0b48c. Builder itself exercised and reverted all three test-strategy cases (baselined-passes-silently, new-violation-still-errors, stale-entry-errors) before committing. Coordinator independently verified: Read both files in full; re-ran `bash test/test-parity.sh` (75 passed, 0 failed, exit 0 — fully green); did my OWN independent spot-check by deleting the CLAST_MACHINE baseline row, confirming the real violation reappeared as a hard failure (exit 1, 72 passed/1 failed), then restored the file and confirmed a byte-identical diff and green run again; confirmed `direnv exec . make lint` exit 0; confirmed commit af0b48c arrived at origin via empty `git log origin/beau/bds-82-porcelain-parity..HEAD`; confirmed clean working tree. No escalations.

**Task 5 (todo 546, Builder-05/884) — suites=() registration: DONE. All 5 Builder tasks complete.** One-line addition to test/test-clast.sh's suites=() array, commit bd74293df4669e9b939317d90eb4d4b3050fb31e. Coordinator independently verified: grep confirms test/test-parity.sh is in the array; `direnv exec . make test` exits 0 with "all test suites passed" (27 suite headers ran including `== test/test-parity.sh ==`); `direnv exec . make lint` exits 0; commit confirmed arrived at origin; `git show HEAD --stat` confirms exactly a 1-line diff to test/test-clast.sh, nothing else touched.

**Coordinator-run proofs (post-Builder, mandatory per Orchestrator mandate — "a guard never observed to fail is not a guard"):**
- **Fail-closed proof**: appended `  --bogus-flag  a flag that isn't in the manifest.` to `_clast_wake_usage`'s `Flags:` block in `lib/clast/clast-porcelain-subcommands/wake.bash`. `bash test/test-parity.sh` → assertion1 A **ERRORed** naming `--bogus-flag` and `wake`, exit 1, NOT suppressed by the baseline (75 passed, 1 failed). Reverted via `git checkout --`; confirmed clean tree; re-ran → 75 passed, 0 failed, exit 0.
- **Ratchet proof**: temporarily removed the `CLAST_WAKE_AUTODISMISS_NOOP` row from `test/parity-known-red.tsv`. `bash test/test-parity.sh` → that specific violation reappeared as a real `FAIL`, exit 1 — proving the baseline isn't a blanket mute. Restored the file (byte-identical diff confirmed); re-ran → green again.
- Final `direnv exec . make test` exit 0 ("all test suites passed"), `direnv exec . make lint` exit 0, `git status --short` clean (only pre-existing session-start `.wip.yaml`/workplan entries).

## Step-07 retro

- 5/5 Builder tasks landed and independently verified by the Coordinator (content read end-to-end, remote-push confirmed, test+lint independently re-run for every task) — plus both mandatory proofs (fail-closed, ratchet) run and recorded directly by the Coordinator after the last Builder closed.
- Shipping-criteria check (`git diff --stat bff33db9~1..HEAD`): 4 files changed — `test/parity.tsv`, `test/parity-known-red.tsv`, `test/test-parity.sh`, `test/test-clast.sh` — 481 insertions, 0 deletions. `skills/`, `lib/`, `docs/` all confirmed untouched (no diff under those paths across the whole step).
- **One escalation, correctly handled end-to-end**: Builder-03 hit 7 real undocumented-env-var violations instead of the workplan's expected 4 (CLAST_LIB/CLAST_JSON/CLAST_COMMAND_MARKER_RE weren't covered by the stated exclusions, despite the workplan's own prose already calling them dispatcher-internal). Builder-03 refused to force-fit the count and escalated instead of silently widening the exclusion on its own authority — exactly the behavior this initiative's guardrails exist to produce. Orchestrator ruled the exemptions should be manifest data (a new self-expiring `internal` kind), not hardcoded bash, extending the existing baseline philosophy consistently. Root cause (a genuine contradiction between Decision 6's prose and its coded exclusion list) was recorded and fixed in the workplan, not just routed around.
- Deviations from the original workplan: the baseline disposition changed from the workplan's original "ship red, record in commit message" recommendation to the Orchestrator's "self-expiring baseline, fully green at landing" ruling — this was an explicit, reasoned override communicated before build started (not a build-time surprise), and the workplan was updated accordingly before Phase 2 began. The `internal` manifest kind (Decision 6's mid-build amendment) was the one genuine build-time addition to the design, driven by Builder-03's escalation.
- No retries, no dead processes, no operator-engagement conflicts this step. Every idle-timer wake correctly distinguished genuine completion from between-step lulls (confirmed via explicit final-report comments + independent re-verification every time, per the liveness-and-report gate).
- The guard's own dogfooding value showed up immediately: assertion 1's bidirectional check caught `CLAST_WAKE_AUTODISMISS_NOOP` missing from wake's own `--help` on its very first run (a real, previously-unknown drift, now ticketed as BDS-101) — direct evidence the guard catches the class of bug (`wake --auto` landing without skill parity) that opened this entire initiative.
- **This closes the porcelain-parity initiative** — BDS-89 was the last of six sub-issues under BDS-82, and all six are now shipped.

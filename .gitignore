# Local config / runtime
.test-tmp/
result
result-*
.direnv/

# wip: derived tracker sync state, rewritten on every `wip sync`
.wip/tracker-cache.json

# Editor
*.swp
.idea/
.vscode/

# OS
.DS_Store
Thumbs.db

# Preventative guard: never let a wip-plugin-style checkout artifact
# (commands/, agents/, .claude-plugin/README.md) be picked up as
# clast's own plugin content.
/commands/
/agents/
/.claude-plugin/README.md
